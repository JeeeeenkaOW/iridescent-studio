// =========================================================
// PEARL — the original. Soft cool iridescence on warm silver.
// =========================================================
import { mergeMaterial } from './schema.js';

export default mergeMaterial({
  name: 'Pearl',
  palette: {
    phase: [0.00, 0.18, 0.42],
  },
  // Pearl uses DEFAULTS for everything else — it IS the default look.
});
