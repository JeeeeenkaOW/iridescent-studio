// =========================================================
// ARCTIC — pale blue/violet shifts. Only palette differs from Pearl.
// =========================================================
// Future tuning targets: cooler base, brighter highlights, softer flow.
//
import { mergeMaterial } from './schema.js';

export default mergeMaterial({
  name: 'Arctic',
  palette: {
    phase: [0.50, 0.55, 0.60],
  },
});
